<!--components/barner/banner.wxml-->
<view class="container">
  <block  wx:for="{{titles}}" wx:key="index">
    <view class="item {{currentIndex==index?'active':''}}" bind:tap="handleClick" data-index="{{index}}">{{item}}</view>
  </block>
</view>
